# AGENTS.md

## 目的

このファイルは、このプロジェクトでCodexが常に守る固定ルールを定義します。

個別機能の実装内容は、その都度のユーザー指示に従ってください。
このファイル自体は、特定の役・UI・機能の追加を指示するものではありません。

## 情報の優先順位

- 現在作業中のローカル環境を正とします。
- リポジトリの選択や切り替えを指示しないでください。
- 作業前に、対象コード、関連テスト、関連データを実際に確認してください。
- GitHub、古い文書、過去の指示とローカル実装が異なる場合は、ローカル実装と最新のユーザー指示を優先してください。
- 推測だけで、機能の有無や現在の設計を断定しないでください。

作業開始時は、原則として次を確認してください。

```text
git status --short --untracked-files=all
git diff
git diff --stat
```

## 既存変更の保護

未コミット変更と未追跡ファイルを含め、既存のローカル変更はすべて維持してください。

次の操作で既存変更を破棄してはいけません。

- `git reset`
- `git restore`
- `git checkout`によるファイル復元
- 強制的なブランチ切り替え
- 未確認ファイルの上書き
- ユーザーが作業中の変更の巻き戻し

自分が変更していない差分を見つけても、勝手に整理・修正・削除しないでください。
目的の実装に必要な箇所だけを変更してください。

## プロジェクト概要

- Unity 6.3 LTS / 6000.3.x
- 2D / Universal Render Pipeline
- Windowsを優先
- Unity Canvas + TextMeshPro
- 特殊能力を含む麻雀ゲームのプロトタイプ

現在の実装には、複数席を前提としたターン進行、ツモ、打牌、ツモ和了、ロン、リーチ、ダブルリーチ、一発、フリテン、役判定、必殺技、開発ログなどが含まれます。

麻雀の全ルールが完成しているとは限りません。
現在の機能と制約は、コードとテストを確認して判断してください。

## 現在の意図的な制約

次の仕組みは、個別タスクで明示されない限り、存在する前提で実装しないでください。

- チー、ポン、カン
- 副露面子、暗槓、明槓を保持する正式な面子モデル
- ドラ、裏ドラ
- 符計算
- 正式な点数計算と支払い
- 本場、供託、流局時テンパイ料、チョンボ
- 複数ロン処理
- オンライン対戦

手全体の`IsClosed`は、各面子の明暗や副露状態を表す正式なモデルではありません。

副露や槓が必要な機能では、部分的なフラグだけを足して済ませず、面子、牌数、山、進行への影響を含めて設計を確認してください。

## 責務分離

既存の責務境界を維持してください。

- `MahjongGameFlow`はゲーム進行を調整する
- Domainクラスは状態と値を保持する
- Service / Evaluatorは具体的な処理やルール判定を担当する
- UIは表示と入力要求を担当する
- Notifierは通知だけを担当する
- ログは観測だけを担当する

Notifier、UI、ログ処理にゲーム進行の決定を持たせないでください。
既存サービスで扱える処理を、`MahjongGameFlow`やUIへ重複実装しないでください。

## ツモ・打牌・必殺技

- 牌を引く処理は既存の`DrawService`を通してください。
- 山から各機能が直接牌を取り除かないでください。
- スキルでも、山に存在しない牌を生成しないでください。
- 打牌は既存の`DiscardService`と`DiscardRecord`の流れを維持してください。
- 捨て牌には実行席、牌、ターン情報を残してください。
- 必殺技の発動判定と効果解決を分離してください。
- 発動済み効果は、現在の`ActiveSkillEffect`や予約機構に沿って保持してください。
- 効果の登録、解決、消費、失効をログで追跡可能にしてください。
- 指定されていない割り込みタイミングや反応UIを勝手に追加しないでください。

## 和了形・役判定

既存の候補単位の評価構造を維持してください。

- 和了形解析と役判定を混同しない
- 通常形、七対子、国士無双などの候補を独立して評価する
- 同じ牌姿の複数解釈を早期に1候補へ決定しない
- 待ち形や和了牌の配置が必要な役は、完成14枚だけで推測しない
- 必要に応じて和了前の手牌と和了牌を分けて判定する

役の表示名、翻数、有効・無効、役満設定は、原則として`YakuDefinitionCatalog`を使用してください。
`YakuKind`の既存数値を変更しないでください。

特殊役を別の`YakuKind`として扱う場合は、その識別を維持してください。
フォールバックが必要な場合は、特殊役から通常役への一方向だけにしてください。

役満が成立した候補では、既存仕様に従って通常役を除外し、同時成立する複数の役満は保持してください。

役判定層では、役満倍率、ダブル役満の点数化、符、最終支払いを扱わないでください。
特殊役の種類を保持し、倍率と点数は将来の点数計算層で扱える形にしてください。

## Unityデータ

Scene、Prefab、ScriptableObject、ProjectSettingsなどは、目的に必要な場合だけ編集してください。

- YAML全体の不要な再保存や並べ替えを避ける
- 本番用`YakuDefinitionCatalog.asset`を、テストを通すためだけに編集しない
- 役判定テストでは、可能な限り動的なテスト用カタログを使う
- Inspector設定が必要なら、変更内容または手順を報告する
- 新しいUnity管理ファイルでは、既存方針に合わせて`.meta`も扱う
- シリアライズ済みフィールド名の変更は、既存データへの影響を確認する

## 実装方針

- 変更範囲は目的を満たす最小限にする
- 周辺コードの命名、構造、ガード節、テスト形式に合わせる
- 同じ分類や走査は、読みやすい範囲で共通化する
- 責務が異なる処理を、行数削減だけのためにまとめない
- 将来使うかもしれないという理由だけで大規模な抽象化を追加しない
- 無関係なリファクタリング、整形、改名、ファイル移動を行わない
- 既存の公開APIやシリアライズ形式を不必要に変更しない
- 既存enum、定数、ヘルパーを優先して再利用する

一時的な制約には、必要に応じて次を使用してください。

```csharp
// PROTOTYPE:
```

コメントは明白な処理の説明ではなく、暫定制約や理由の記録に使ってください。

## テスト

変更した振る舞いには、既存形式に合わせたテストを追加・更新してください。

必要な範囲で次を確認してください。

- 代表的な成立例
- 代表的な不成立例
- 境界条件
- 排他関係
- 定義が未登録または無効な場合
- 既存機能への回帰

代表例で十分な場合は、入力の全組み合わせを機械的に列挙しないでください。
テストを通すために、本番データや本番シーンを不自然に変更しないでください。

## 検証

変更後は、利用可能な範囲で次を実行してください。

```text
dotnet build Assembly-CSharp.csproj --no-restore
dotnet build MahjongPrototype.EditModeTests.csproj --no-restore
git diff --check
git status --short --untracked-files=all
```

構成が変わっている場合は、現在存在する同等の検証を選んでください。

Unity Test Runnerを利用できる場合は、関連するEditModeテストを実行してください。

`Temp/UnityLockfile`が存在する場合は、Unity Editorの強制終了、ロックファイルの削除、別Unityプロセスの強制起動を行わないでください。

Unityテストを実行できなかった場合は、その理由を報告してください。
`dotnet test`でUnityテスト名、件数、結果が確認できない場合は、Unity Test Runner成功として報告しないでください。

## 作業中の判断

不明点は、まずコード、テスト、関連文書を調査してください。
小さな判断は既存パターンに合わせて進め、作業を止める重大な仕様不明点だけを質問してください。

個別タスクが簡潔でも、このファイルの固定ルールを適用してください。
このファイルにない受け入れ条件を推測して、大量に追加しないでください。

## 完了報告

報告は簡潔にまとめてください。

- 実装内容
- 変更ファイル
- 追加・更新したテスト
- ビルドとテスト結果
- 実行できなかった検証と理由
- 手動確認やInspector設定
- 既知の制約または残作業

タスク指示を繰り返す長大なチェックリストは不要です。
確認できていないことを成功したと報告しないでください。
