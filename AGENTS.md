# AGENTS.md

## 目的

このファイルは、このプロジェクトでCodexが常に守る基本ルールを定義します。
個別機能の仕様と受け入れ条件は、その都度のユーザー指示に従ってください。

このファイル自体は、特定の機能追加や個別タスクの実装を指示するものではありません。

一般的なC# / Unityの規律は汎用性を基準に整理します。
一方、このプロジェクト固有のクラス、状態、処理経路に関する記述は、
今後の実装で既存構造の誤用や重複実装を防ぐ必要がある場合に維持してください。

## 作業環境

- 現在作業中のローカル環境を正とします。
- リポジトリの選択や切り替えを指示しないでください。
- 作業前に、対象コード、関連テスト、関連データを確認してください。
- GitHub、古い文書、過去の指示より、ローカル実装と最新のユーザー指示を優先してください。
- 推測だけで、機能の有無や現在の設計を断定しないでください。

作業開始時は原則として次を確認してください。

```text
git status --short --untracked-files=all
git diff
git diff --stat
```

## 既存変更の保護

未コミット変更と未追跡ファイルを含め、既存のローカル変更を維持してください。
自分が変更していない差分を、整理、修正、削除、上書きしないでください。

次の操作で既存変更を破棄してはいけません。

- `git reset`
- `git restore`
- `git checkout`によるファイル復元
- 強制的なブランチ切り替え
- 未確認ファイルの上書き

目的の実装に必要な箇所だけを変更してください。

## プロジェクト概要

- Unity 6.3 LTS / 6000.3.x
- Universal Render Pipeline
- Windows優先
- Unity Canvas + TextMeshPro
- 3D牌・卓上表示を含む`UI3D`
- 特殊能力を含む麻雀ゲームのプロトタイプ

麻雀の全ルールが完成しているとは限りません。
現在の機能と制約は、コードとテストを確認して判断してください。

## 現在の主な実装状況

チー・ポン・カン（大明槓・暗槓・加槓を含む）の副露判断と局進行は、
人間のローカル操作を前提に実装済みです。

次は未実装です。

- CPUによる副露判断
- 通信対戦時の副露判断
- 符計算
- 正式な点数計算と支払い

これらが未実装であることを理由に、既存の副露・槓処理自体を未実装扱いしないでください。

個別タスクで明示されない限り、次を完成済みの前提で実装しないでください。

- ドラ、裏ドラ
- 本場、供託、流局時テンパイ料、チョンボ
- 複数ロン処理
- オンライン対戦
- 副露・槓を含む全役、符、点数への完全統合

## 責務境界

既存の責務境界を維持してください。

- `MahjongGameFlow`：ゲーム進行の調整
- Domain：状態と値
- Service / Evaluator：処理とルール判定
- UI：表示と入力要求
- Notifier：通知
- Logging：観測

UI、Notifier、Loggingにゲーム進行の決定を持たせないでください。
既存サービスで扱える処理を、`MahjongGameFlow`やUIへ重複実装しないでください。

## 既存のゲーム処理経路

- UIやDecision Providerへ渡す判断には、Authorityが作成した不変な`DecisionRequest` / `ReactionDecisionRequest`を使用してください。
- 回答は既存のDecision Response経路を通し、UIから局状態を直接確定しないでください。
- 捨て牌由来の副露では、`DiscardRecord.Id`と`DiscardClaims`の整合性を維持してください。
- 副露・槓の状態には、既存の`PlayerMeld`と`PlayerSeat.Melds`を使用してください。
- `PlayerMeld`の既存フィールドは用途を確認して使い分け、同じ情報を別構造へ重複保持しないでください。
- 加槓への反応を、河の捨て牌への反応として扱わないでください。
- 牌を引く処理は`DrawService`、打牌は`DiscardService`と`DiscardRecord`の経路を通してください。
- 山に存在しない牌を、通常処理やスキルから生成しないでください。
- 副露、槓、スキルでは、牌数、山、嶺上牌、手番、リーチ、一発、フリテン、反応窓への影響を確認してください。

## 和了形と役判定

既存の候補単位の評価構造を維持してください。

- 和了形解析と役判定を混同しない
- 通常形、七対子、国士無双などを独立候補として評価する
- 同じ牌姿の複数解釈を早期に一つへ決めない
- 待ち形や和了牌の配置が必要な役は、完成14枚だけで推測しない
- 必要に応じて和了前の手牌と和了牌を分ける

役の定義は原則として`YakuDefinitionCatalog`を使用してください。
役判定層で符、最終点数、支払いを扱わないでください。
既存enumの数値や識別を不必要に変更しないでください。
役満が成立した候補では通常役を除外し、同時成立する複数の役満は保持してください。
特殊役から通常役へのフォールバックが必要な場合は一方向だけにしてください。

## Unityデータ

Scene、Prefab、ScriptableObject、ProjectSettingsは、目的に必要な場合だけ編集してください。

- YAML全体の不要な再保存や並べ替えを避ける
- テストのためだけに本番データや本番Sceneを変更しない
- Inspector設定が必要なら変更内容または手順を報告する
- 新しいUnity管理ファイルでは既存方針に合わせて`.meta`も扱う
- シリアライズ済みフィールド名の変更は既存データへの影響を確認する

## 実装方針

- 変更範囲は、指示された目的と受け入れ条件を満たす最小限にしてください。
- 周辺コードの命名、構造、ガード節、テスト形式に合わせてください。
- 既存の状態、サービス、ヘルパー、処理経路を優先して再利用してください。
- 同じ事実を複数の状態やフラグで重複保持しないでください。
- 同じ判定や状態更新を複数箇所へ重複実装しないでください。
- 無関係なリファクタリング、整形、改名、ファイル移動を行わないでください。
- 公開API、シリアライズ形式、既存の互換経路を不必要に変更しないでください。
- このファイルや個別指示にない受け入れ条件を推測して追加しないでください。

## 過剰設計の防止

必要な責務分離や安全対策は維持しつつ、目的に対して説明できない構造や防御コードを増やさないでください。

- 将来使うかもしれないという理由だけで、抽象化、インターフェース、設定項目、フォールバック、互換経路を追加しないでください。
- 単一の利用箇所しかない小さな処理を、行数削減だけを理由に新しいクラスや細かなヘルパーへ分割しないでください。
- 一方で、独立したルール判定、複数箇所で使う処理、単体テストすべき処理は、既存の責務境界に従って分離してください。
- 大きなファイルであることだけを理由に、今回の目的と無関係な分割や再設計を行わないでください。
- 既存状態から導出できる値のために、新しい状態やフラグを追加しないでください。
- 新旧二つの処理経路を残す場合は、互換性や移行上の必要性を確認してください。
- 実装後は変更したコードだけを見直し、挙動を変えずに削除できる重複、未使用コード、不要な状態、分岐、フォールバックを残さないでください。

過剰設計を避けることを理由に、通常必要な設計や安全対策を省略しないでください。
公開APIの境界、外部入力、再入可能なイベント、状態不変条件、非同期・遅延処理、
シリアライズ互換性など、明確な理由があるガードや責務分離は維持してください。

## C# / Unityの基本規律

- enumの数値明示は、互換性、シリアライズ、外部契約、ビットフラグ、順序計算などの理由がある場合に使用してください。
- 公開APIの境界、外部入力、再入可能なイベントハンドラ、状態不変条件の維持に必要なnullチェックやearly returnは追加してください。
- 呼び出し元でnon-nullが保証される内部呼び出しへ、同じnullチェックを重複して追加しないでください。
- `try-catch`は、例外の回復、変換、隔離、記録が必要な場合に使用してください。何も行わず例外を握り潰す処理は追加しないでください。
- `Update`、`LateUpdate`、アニメーションループなどの高頻度処理では、不要なLINQ、コレクション生成、文字列生成、クロージャキャプチャを追加しないでください。
- `UnityEngine.Object`派生型の生存確認には、原則としてUnityの`== null` / `!= null`を使用してください。
- 一時的な`Debug.Log`は完了前に削除してください。継続的な観測には既存の`DevLog`、Development Build限定処理、または既定で無効なデバッグフラグを使用してください。

一時的な制約には必要に応じて`// PROTOTYPE:`を使用してください。
コメントは処理内容の言い換えではなく、暫定制約や明白でない設計理由の記録に使ってください。

## テスト

変更した振る舞いには、既存形式に合わせたテストを追加・更新してください。

必要な範囲で次を確認してください。

- 代表的な成立例と不成立例
- 境界条件
- 排他関係
- 既存機能への回帰

代表例で十分な場合は、入力の全組み合わせを機械的に列挙しないでください。
テストのために本番データや本番Sceneを不自然に変更しないでください。

## 検証

変更後は、利用可能な範囲で次を実行してください。

```text
dotnet build Assembly-CSharp.csproj --no-restore
dotnet build MahjongPrototype.EditModeTests.csproj --no-restore
git diff --check
git status --short --untracked-files=all
```

Unity Test Runnerを利用できる場合は、関連するEditModeテストを実行してください。

`Temp/UnityLockfile`が存在する場合は、Unity Editorの強制終了、ロックファイル削除、別Unityプロセスの強制起動を行わないでください。
Unityテストを実行できなかった場合は理由を報告してください。
`dotnet test`だけでUnity Test Runner成功として報告しないでください。

## 作業中の判断

不明点は、まずコード、テスト、関連文書を調査してください。
小さな判断は既存パターンに合わせ、作業を止める重大な仕様不明点だけを質問してください。

## 完了報告

次を簡潔に報告してください。

- 実装内容と変更ファイル
- 追加・更新したテスト
- ビルドとテスト結果
- 実行できなかった検証と理由
- Inspector設定、既知の制約、残作業

確認できていないことを成功したと報告しないでください。
